[project]
name = "graphaide"
version = "0.1.0"
description = "Data processor and visualizer"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "matplotlib>=3.10.8",
    "pandas>=2.3.3",
    "tomli>=2.3.0",
]

[dependency-groups]
dev = [
    "black>=25.12.0",
    "jupyter>=1.1.1",
    "pytest>=9.0.2",
]
